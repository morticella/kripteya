import { Building} from './building.model';

export interface Buildings {
  _id: string;
  building: Building;
  // address: string;
  // info: string;
  // rooms: Room[];
  // notice: Date;
  // booked: Date;
}
